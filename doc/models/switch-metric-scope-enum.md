
# Switch Metric Scope Enum

Scope level used when querying switch metrics. enum: `site`, `switch`

## Enumeration

`SwitchMetricScopeEnum`

## Fields

| Name |
|  --- |
| `site` |
| `switch` |

## Example

```go
package main

import (
    "mistapi/models"
)

func main() {
    switchMetricScope := models.SwitchMetricScopeEnum_SITE

}
```

