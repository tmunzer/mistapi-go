
# Gateway Template Tunnel Mode Enum

enum: `active-active`, `active-standby`

## Enumeration

`GatewayTemplateTunnelModeEnum`

## Fields

| Name |
|  --- |
| `active-active` |
| `active-standby` |

