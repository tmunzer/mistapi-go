
# Wxlan Rule Action Enum

type of action, allow / block. enum: `allow`, `block`

## Enumeration

`WxlanRuleActionEnum`

## Fields

| Name |
|  --- |
| `allow` |
| `block` |

## Example

```
allow
```

