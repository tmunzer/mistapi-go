
# Wlan Auth Owe Enum

if `type`==`open`. enum: `disabled`, `enabled` (means transition mode), `required`

## Enumeration

`WlanAuthOweEnum`

## Fields

| Name |
|  --- |
| `disabled` |
| `enabled` |
| `required` |

