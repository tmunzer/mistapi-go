
# Tunnel Config Protocol Enum

Only if `provider`==`custom-ipsec`. enum: `gre`, `ipsec`

## Enumeration

`TunnelConfigProtocolEnum`

## Fields

| Name |
|  --- |
| `gre` |
| `ipsec` |

