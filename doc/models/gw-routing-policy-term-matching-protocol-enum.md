
# Gw Routing Policy Term Matching Protocol Enum

enum: `aggregate`, `bgp`, `direct`, `ospf`, `static` (SRX Only)

## Enumeration

`GwRoutingPolicyTermMatchingProtocolEnum`

## Fields

| Name |
|  --- |
| `aggregate` |
| `bgp` |
| `direct` |
| `ospf` |
| `static` |

## Example

```go
package main

import (
    "mistapi/models"
)

func main() {
    gwRoutingPolicyTermMatchingProtocolEnum := models.GwRoutingPolicyTermMatchingProtocolEnum_AGGREGATE

}
```

