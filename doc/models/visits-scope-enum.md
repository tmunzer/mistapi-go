
# Visits Scope Enum

enum: `map`, `rssizone`, `site`, `zone`

## Enumeration

`VisitsScopeEnum`

## Fields

| Name |
|  --- |
| `map` |
| `rssizone` |
| `site` |
| `zone` |

## Example

```go
package main

import (
    "mistapi/models"
)

func main() {
    visitsScope := models.VisitsScopeEnum_SITE

}
```

