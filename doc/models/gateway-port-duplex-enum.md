
# Gateway Port Duplex Enum

enum: `auto`, `full`, `half`

## Enumeration

`GatewayPortDuplexEnum`

## Fields

| Name |
|  --- |
| `auto` |
| `full` |
| `half` |

## Example

```go
package main

import (
    "mistapi/models"
)

func main() {
    gatewayPortDuplex := models.GatewayPortDuplexEnum_HALF

}
```

