
# Gateway Port Wan Type Enum

if `usage`==`wan`. enum: `broadband`, `dsl`, `lte`

## Enumeration

`GatewayPortWanTypeEnum`

## Fields

| Name |
|  --- |
| `broadband` |
| `dsl` |
| `lte` |

