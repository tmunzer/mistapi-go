
# Gateway Port Wan Type Enum

Only if `usage`==`wan`. enum: `broadband`, `dsl`, `lte`

## Enumeration

`GatewayPortWanTypeEnum`

## Fields

| Name |
|  --- |
| `broadband` |
| `dsl` |
| `lte` |

