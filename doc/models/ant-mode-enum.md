
# Ant Mode Enum

Antenna Mode for AP which supports selectable antennas. enum: `external`, `internal`

## Enumeration

`AntModeEnum`

## Fields

| Name |
|  --- |
| `external` |
| `internal` |

## Example

```
external
```

