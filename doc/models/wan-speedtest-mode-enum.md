
# Wan Speedtest Mode Enum

Controls whether Marvis/scheduler can run speedtest on this port. enum: `auto`, `enabled`, `disabled`

## Enumeration

`WanSpeedtestModeEnum`

## Fields

| Name |
|  --- |
| `auto` |
| `enabled` |
| `disabled` |

## Example

```
auto
```

